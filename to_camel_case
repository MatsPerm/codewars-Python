def to_camel_case(text):
    """Конвертирует строку text с дефисами и подчеркиваниями между словами 
    (типа 'xxx-YYY-zzz-...' or 'xxx_yyy_ZZZ_...') в слитное написание слов, 
    слова начинаются с большой буквы, кроме первого (первое слово сохраняет
    свою начальную букву).
    Результат типа 'xxxYyyZzz...'"""

    if text == '': return ''
    return text[0]+text.title().replace('_', '').replace('-', '')[1:]
