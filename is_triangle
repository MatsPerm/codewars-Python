def is_triangle(a, b, c):
  """
  Проверяет, могут ли длины a, b, c составить стороны треугольника. 
  Возвращает Ложь, если 
  1) хотя бы одна сторона является отрицательным числом;
  2) самая большая длина больше суммы двух меньших (стороны не могут сомкнуться).
  """
  
    if a<=0 or b<=0 or c<=0: return False
    a, b, c = sorted([a, b, c])
    return a + b > c
