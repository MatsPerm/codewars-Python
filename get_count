def get_count(sentence):
  """
  Подсчитывает количество гласных ('у' не считается) в строке.
  На входе только латиница в нижнем регистре.
  """
  
    count = 0
    for i in ['a', 'e', 'i', 'o', 'u']:
        count += sentence.count(i)
    return count
